import XCTest
@testable import SuperMarketUI

final class SuperMarketUITests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SuperMarketUI().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
